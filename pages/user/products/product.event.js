export default class ProductEvent {
  onLoadProduct() {}

  onSortProduct(data) {}

  onGetProduct(id) {
    // Điều hướng tới sản phẩm chi tiết
  }
  onFavoute(id) {
    // Lưu sản phẩm yêu thích
  }
  addToCart(id) {
    // Thêm sản phẩm vào giỏ hàng
  }

  onBuyNow(id) {
    // Mua sản phẩm ngay
  }

  onSearchProducts(searchValue) {
    // Tìm kiếm sản phẩm theo tên sp
  }
}
